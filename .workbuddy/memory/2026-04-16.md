# 2026-04-16 工作日志

## 部署配置（2026-04-16 晚）
- 后端数据库从 sql.js 迁移到 Node.js 22+ 内置 node:sqlite（无需编译，云端兼容）
- 创建 railway.json（后端部署配置，Node.js 22+）
- 创建 vercel.json（前端部署配置，Root Directory=frontend）
- 修复 admin.html 中硬编码的 localhost:3001，改为自动判断环境
- 创建 .gitignore（排除 node_modules、database.sqlite、uploads）
- 初始化 Git 仓库并完成 initial commit
- 创建 部署指南.md（含 GitHub + Railway + Vercel 完整步骤）
- 部署方案：前端→Vercel（永久免费）/ 后端→Railway（$5/月免费额度）

## 完成内容
- 将用户提供的 LOGO（深绿底+米白 "Smoking Accessories Wholesale" 大字）用SVG精确还原，保存为 `frontend/public/logo.svg`
- 更新 `Header.tsx`：左侧换成真实LOGO图片 + 公司全名（HONG KONG COOKIES TRADING LIMITED），TRADING LIMITED 用金色(#D4A574)显示
- 更新 `Home.tsx` Hero Banner：左右分栏布局，右侧展示LOGO大图
- 更新 `Home.tsx` Footer：加入LOGO图片+公司全名
- 更新 `favicon.svg` 为品牌图标
- 用户提供LOGO图片无法从系统缓存找到，已用SVG重建视觉效果；如用户希望用真实图片，需手动将图片保存为 `frontend/public/logo.jpg` 并在代码中将 `/logo.svg` 替换为 `/logo.jpg`
